using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Testmanything : MonoBehaviour
{
    // Start is called before the first frame update
    //public GameObject target;
    //public float curent, targets;
    //public Vector3 stp;
    //void Start()
    //{
    //    stp = transform.position;
    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    if (Input.GetKeyDown(KeyCode.A)) //targets = targets == 0 ? 1 : 0;
    //    {
    //        //PlayerPrefs.SetFloat("ngu", 10);
    //        //PlayerPrefs.DeleteKey("ngu");
    //        Debug.Log("ngu1 "+ Math.Round(9.51f));
    //        Debug.Log("ngu2 "+ Math.Round(9.49f));
    //    }
    //    //curent = Mathf.MoveTowards(curent, targets, 0.1f * Time.deltaTime);
    //    //transform.position = Vector3.Lerp(transform.position, target.transform.position, curent);
    //    //curent += Time.deltaTime;
    //    //float percent = curent / targets;
    //    //transform.position = Vector3.Lerp(stp, target.transform.position, percent);

    //}
}
